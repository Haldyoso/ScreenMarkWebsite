Presence of this file tells GitHub Pages to publish the artifact as-is instead
of running it through Jekyll, which silently drops every path starting with an
underscore — including Next's /_next/ directory, i.e. all JS and CSS.
